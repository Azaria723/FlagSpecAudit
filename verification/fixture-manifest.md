# Fixture manifest

All fixtures are original synthetic examples authored for FlagSpecAudit. They do not make claims about an external project. Exact byte lengths, Git blob SHA-1 values, SHA-256 values, and the immutable public commit will be recorded after the first GitHub push.
